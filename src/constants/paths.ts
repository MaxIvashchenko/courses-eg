interface IPaths {
  main: string;
  legal: string;
  profile: string;
  signIn: string;
  signUp: string;
  modules: string;
  module: string;
  block: string;
}

const paths: IPaths = {
  main: '/',
  legal: 'legal',
  profile: '/profile',
  signIn: '/signIn',
  signUp: '/signUp',
  modules: '/modules',
  module: '/module',
  block: '/block'
};

export default paths;
