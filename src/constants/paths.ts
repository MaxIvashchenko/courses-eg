interface IPaths {
  main: string;
  legal: string;
  dashboard: string;
  signIn: string;
  signUp: string;
}

const paths: IPaths = {
  main: '/',
  legal: 'legal',
  dashboard: '/dashboard',
  signIn: '/signIn',
  signUp: '/signUp'
};

export default paths;
