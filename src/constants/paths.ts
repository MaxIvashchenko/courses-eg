interface IPaths {
  main: string;
  legal: string;
  profile: string;
  signIn: string;
  signUp: string;
  program: string;
}

const paths: IPaths = {
  main: '/',
  legal: 'legal',
  profile: '/profile',
  signIn: '/signIn',
  signUp: '/signUp',
  program: '/program'
};

export default paths;
